theme: jekyll-theme-midnight
defaults:
  -
    scope:
      path: ""
      type: "patterns"
    values:
      layout: "pattern"
collections:
  patterns:
    output: true
    permalink: /:categories/:name
    layout: "pattern"
