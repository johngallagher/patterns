theme: jekyll-theme-slate
defaults:
  -
    scope:
      path: ""
      type: "patterns"
    values:
      layout: "pattern"
collections:
  patterns:
    output: true
    permalink: /:categories/:name
    layout: "pattern"
